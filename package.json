{
  "private": true,
  "type": "module",
  "description": "Maintainer-only build for dist/numerosis.css and dist/numerosis.js (prebuilt assets published to public/vendor/numerosis by the numerosis-public-assets tag). Not required by consumers — composer packages never run npm.",
  "scripts": {
    "build:app-css": "tailwindcss -i ./resources/theme-src/app.css -o ./dist/numerosis.css --minify",
    "build:app-js": "vite build",
    "build": "npm run build:app-css && npm run build:app-js"
  },
  "devDependencies": {
    "@stripe/stripe-js": "^4.10.0",
    "@tailwindcss/cli": "^4.1.17",
    "playwright": "^1.62.1",
    "tailwindcss": "^4.1.17",
    "vite": "^7.0.5"
  }
}
