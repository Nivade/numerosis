<?php

declare(strict_types=1);

// No routes: the package registers `/` on each central domain and on the
// tenant group, and the tenant one replaces anything declared here. Browse the
// harness on http://localhost:8000, never the loopback IP.
